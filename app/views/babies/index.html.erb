<h1>すくすく</h1>

<%= image_tag 'publicdomainq-0006718ljo.jpeg', style: 'width: 250px; height: auto;' %>

<ul>
  <% @babies.each do |baby| %>
    <li>
      <%= link_to baby.name, baby_path(baby) %>
    </li>
  <% end %>
</ul>

<%= link_to "新しい赤ちゃん", new_baby_path, class: "btn-success" %>
